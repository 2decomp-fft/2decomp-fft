file(GLOB files_test2d test2d.f90)
set(case "test2d")
set(case_dir "${test_dir}/${case}")
CreateMPITest("${case_dir}" "${case}" "${files_test2d}" "" "" "" "1")

file(GLOB files_timing2d_real timing2d_real.f90)
set(case "timing2d_real")
set(case_dir "${test_dir}/${case}")
CreateMPITest("${case_dir}" "${case}" "${files_timing2d_real}" "" "" "" "1")

file(GLOB files_timing2d_complex timing2d_complex.f90)
set(case "timing2d_complex")
set(case_dir "${test_dir}/${case}")
CreateMPITest("${case_dir}" "${case}" "${files_timing2d_complex}" "" "" "" "1")

if (NOT BUILD_TARGET MATCHES "gpu")
  file(GLOB files_decomp_pool decomp_pool.f90)
  set(case "decomp_pool")
  set(case_dir "${test_dir}/${case}")
  CreateMPITest("${case_dir}" "${case}" "${files_decomp_pool}" "" "" "" "1")
endif()
