if (IO_BACKEND MATCHES "adios2")
  set(file_list "${CMAKE_SOURCE_DIR}/examples/io_adios/adios2_config.xml" )
else()
  set(file_list "")
endif (IO_BACKEND MATCHES "adios2")

file(GLOB files_test grad3d.f90)
set(case "grad3d")
set(case_dir "${test_dir}/${case}")
CreateMPITest("${case_dir}" "${case}" "${files_test}" "" "" "${file_list}" "1")
