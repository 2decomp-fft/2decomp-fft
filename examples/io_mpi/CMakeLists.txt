if (COMPLEX_TESTS)
  set(def_clmpx "COMPLEX_TESTS")
else()
  set(def_clmpx "")
endif()

if (IO_BACKEND MATCHES "adios2")
  set(file_list "${CMAKE_SOURCE_DIR}/examples/io_adios/adios2_config.xml" )
else()
  set(file_list "")
endif (IO_BACKEND MATCHES "adios2")

file(GLOB files_test io_test.f90)
set(case "io_write")
set(case_dir "${test_dir}/io_write_read")
CreateMPITest("${case_dir}" "${case}" "${files_test}" "" "${def_clmpx}" "${file_list}" "1")

file(GLOB files_read io_read.f90)
set(case "io_read")
CreateMPITest("${case_dir}" "${case}" "${files_read}" "" "${def_clmpx}" "${file_list}" "1")

file(GLOB files_var_test io_var_test.f90)
set(case "io_var_test")
set(case_dir "${test_dir}/${case}")
CreateMPITest("${case_dir}" "${case}" "${files_var_test}" "" "${def_clmpx}" "${file_list}" "1")

file(GLOB files_plane_test io_plane_test.f90)
set(case "io_plane_test")
set(case_dir "${test_dir}/${case}")
CreateMPITest("${case_dir}" "${case}" "${files_plane_test}" "" "${def_clmpx}" "${file_list}" "1")

file(GLOB files_bench io_bench.f90)
set(case "io_bench")
set(case_dir "${test_dir}/${case}")
CreateMPITest("${case_dir}" "${case}" "${files_bench}" "" "${def_clmpx}" "${file_list}" "1")

file(GLOB files_visu io_visu.f90)
set(case "io_visu")
set(case_dir "${test_dir}/${case}")
CreateMPITest("${case_dir}" "${case}" "${files_visu}" "" "${def_clmpx}" "${file_list}" "1")

file(GLOB files_mpi_non_blocking io_mpi_non_blocking.f90)
set(case "io_mpi_non_blocking")
set(case_dir "${test_dir}/${case}")
CreateMPITest("${case_dir}" "${case}" "${files_mpi_non_blocking}" "" "${def_clmpx}" "${file_list}" "1")
