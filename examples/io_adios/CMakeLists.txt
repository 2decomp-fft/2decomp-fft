if (COMPLEX_TESTS)
  set(def_clmpx "COMPLEX_TESTS")
else()
  set(def_clmpx "")
endif()

file(GLOB files_adios_test io_test.f90)
set(case "io_adios_write")
set(case_dir "${test_dir}/io_adios_write_read")
set(file_list "adios2_config.xml" )
CreateMPITest("${case_dir}" "${case}" "${files_adios_test}" "" "${def_clmpx}" "${file_list}" "1")

file(GLOB files_adios_read io_read.f90)
set(case "io_adios_read")
set(case_dir "${test_dir}/io_adios_write_read")
CreateMPITest("${case_dir}" "${case}" "${files_adios_read}" "" "${def_clmpx}" "" "1")

file(GLOB files_adios_tmp_test io_tmp_test.f90)
set(case "io_adios_tmp_test")
set(case_dir "${test_dir}/${case}")
set(file_list "adios2_config.xml" )
CreateMPITest("${case_dir}" "${case}" "${files_adios_tmp_test}" "" "${def_clmpx}" "${file_list}" "1")

file(GLOB files_adios_plane_test io_plane_test.f90)
set(case "io_adios_plane_test")
set(case_dir "${test_dir}/${case}")
set(file_list "adios2_config.xml" )
CreateMPITest("${case_dir}" "${case}" "${files_adios_plane_test}" "" "${def_clmpx}" "${file_list}" "1")

file(GLOB files_adios_bench io_bench.f90)
set(case "io_adios_bench")
set(case_dir "${test_dir}/${case}")
set(file_list "adios2_config.xml" )
CreateMPITest("${case_dir}" "${case}" "${files_adios_bench}" "" "${def_clmpx}" "${file_list}" "1")

file(GLOB files_adios_visu io_visu.f90)
set(case "io_adios_visu")
set(case_dir "${test_dir}/${case}")
set(file_list "adios2_config.xml" )
CreateMPITest("${case_dir}" "${case}" "${files_adios_visu}" "" "${def_clmpx}" "${file_list}" "1")
