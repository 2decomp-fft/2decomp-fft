file(GLOB files_test test2d.f90)
include_directories(${CMAKE_SOURCE_DIR}/src)

add_executable(test2d ${files_test})
target_link_libraries(test2d PRIVATE decomp2d)

if (BUILD_TARGET MATCHES "gpu")
  set(example_dir "${PROJECT_BINARY_DIR}/examples/test2d")
  message(STATUS "Example dir ${example_dir}")
  install(FILES bind.sh DESTINATION ${example_dir})
  file(COPY bind.sh DESTINATION ${example_dir})
  add_test(NAME test2d COMMAND ${MPIEXEC_EXECUTABLE} ${MPIEXEC_NUMPROC_FLAG} ${MPIEXEC_MAX_NUMPROCS} ./bind.sh $<TARGET_FILE:test2d>)
else ()
  add_test(NAME test2d COMMAND ${MPIEXEC_EXECUTABLE} ${MPIEXEC_NUMPROC_FLAG} ${MPIEXEC_MAX_NUMPROCS} $<TARGET_FILE:test2d>)
endif ()
